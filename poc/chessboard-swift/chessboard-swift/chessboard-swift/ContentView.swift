// ContentView.swift - Root coordinator view for ChessboardApp
import SwiftUI

struct ContentView: View {
    var body: some View {
        LaunchCoordinator()
    }
}

#Preview {
    ContentView()
}
