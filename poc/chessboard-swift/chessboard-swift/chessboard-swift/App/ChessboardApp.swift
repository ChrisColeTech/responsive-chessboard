// ChessboardApp.swift - Our app configuration (not main entry point)
import SwiftUI

// Note: This is not the main entry point anymore - that's handled by chessboard_swiftApp.swift
struct ChessboardAppConfig: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}