{
  "name": "chessboard-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "echo \"Building backend...\" && npm audit fix --force && npm prune --production=false",
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "prod": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Linting backend code...\" && echo \"No linting configured yet\"",
    "clean": "echo \"Cleaning backend...\" && rm -rf node_modules package-lock.json && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^12.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "nodemailer": "^7.0.6",
    "nodemon": "^3.1.10",
    "uuid": "^13.0.0"
  }
}
